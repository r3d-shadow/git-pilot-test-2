name: scan

on:
  push:
    branches:
      - "stage"

jobs:
  sast_scan123456:
    runs-on: ubuntu-latest
    steps:
      - name: Hello from stage!
        run: echo "Hello, sast_scan123456!"
        env:
          SUPER_SECRET: ${{ secrets.SuperSecret }}